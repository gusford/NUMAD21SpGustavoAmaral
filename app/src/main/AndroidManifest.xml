<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.numad21sp_gustavoamaral" >

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_ga"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_ga_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NUMAD21SpGustavoAmaral" >

        <activity
            android:name=".LocatorActivity"
            android:label="@string/title_activity_locator" >
        </activity>
        <activity
            android:name=".LinkCollectorActivity"
            android:label="@string/title_activity_link_collector" />
        <activity android:name=".ButtonGridActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.NUMAD21SpGustavoAmaral.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>